{
  "name": "node-typescript-starter",
  "version": "0.0.1",
  "description": "Simple Node.js typescript starter boilerplate",
  "author": "madinar.cse@gmail.com",
  "keywords": [
    "ts-node",
    "typescript",
    "node",
    "typescript-starter"
  ],
  "license": "MIT",
  "main": "src/index.ts",
  "scripts": {
    "dev": "run-s build && cross-env nodemon",
    "build": "run-s clean && run-p build:tsc",
    "clean": "trash dist",
    "build:tsc": "tsc -p tsconfig.json"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^13.1.2",
    "cross-env": "^6.0.3",
    "nodemon": "^2.0.2",
    "npm-run-all": "^4.1.5",
    "trash-cli": "^3.0.0",
    "ts-node": "^8.5.4",
    "typescript": "^3.7.4"
  }
}
